name: Retail Demand Forecasting CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Train model
      run: |
        python train_model.py

    - name: Predict
      run: |
        python predict.py

    - name: Drift monitoring
      run: |
        python drift_monitor.py

  docker-build:
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build Docker images
      run: |
        docker build -t train_model -f Dockerfile.train .
        docker build -t predict_model -f Dockerfile.predict .
        docker build -t drift_monitor -f Dockerfile.drift .

    # Optional: push images to Docker Hub (requires secrets)
    #- name: Log in to Docker Hub
    #  uses: docker/login-action@v2
    #  with:
    #    username: ${{ secrets.DOCKER_USERNAME }}
    #    password: ${{ secrets.DOCKER_PASSWORD }}
    #
    #- name: Push images
    #  run: |
    #    docker tag train_model yourusername/train_model:latest
    #    docker tag predict_model yourusername/predict_model:latest
    #    docker tag drift_monitor yourusername/drift_monitor:latest
    #    docker push yourusername/train_model:latest
    #    docker push yourusername/predict_model:latest
    #    docker push yourusername/drift_monitor:latest
